{
  "name": "ai-resume-analyzer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "db:generate": "pnpm --filter @ai-resume-analyzer/api db:generate",
    "db:migrate": "pnpm --filter @ai-resume-analyzer/api db:migrate",
    "db:push": "pnpm --filter @ai-resume-analyzer/api db:push",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter @ai-resume-analyzer/api dev",
    "start:api": "pnpm --filter @ai-resume-analyzer/api start",
    "format": "biome format --write .",
    "lint": "biome check .",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
