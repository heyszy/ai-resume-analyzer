# Mock PDF 简历

这个目录用于存放本地测试用的 PDF 简历样本。

## 说明

- `01` 到 `07` 是可被文本解析器提取内容的正常样本。
- `08-损坏文件样本.pdf` 是故意构造的损坏样本，可用于验证 PDF 解析失败分支。

## 生成方式

在仓库根目录执行：

```bash
node scripts/generate-mock-resumes.mjs
```

脚本会覆盖同名文件，便于重复刷新测试数据。
